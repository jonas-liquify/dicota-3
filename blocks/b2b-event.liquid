<div li-block="B2B Event" class="acc-card gap-2-5" {{ block.shopify_attributes }}>
                    {% if block.settings.image_bild != blank %}    <span class="li-guard" li-if="block.settings.image_bild != blank">
    {% if block.settings.image_bild %}
{{ block.settings.image_bild | image_url: width: 600 | image_tag: loading: 'lazy', class: 'acc-card_3-2-image' }}
{% else %}
<img src="{{ 'platzhalter.svg' | asset_url }}" loading="lazy" alt="" class="acc-card_3-2-image">
{% endif %}    </span>
{% endif %}
                    <div class="acc-card_content">{% if block.settings.text_datum_ort != blank %}    <div class="li-guard" li-if="block.settings.text_datum_ort != blank">
    <div class="text-style-subline">
{{ block.settings.text_datum_ort }}
</div>    </div>
{% endif %}<div class="text-size-large">
{{ block.settings.text_heading }}
</div><div class="text-size-regular"> {{ block.settings.textarea_text }} </div></div>
                    {% if block.settings.text_button_text != blank %}    <span class="li-guard" li-if="block.settings.text_button_text != blank">
    <a href="{{ block.settings.url_button_link }}" class="button is-secondary w-button"> <span>{{ block.settings.text_button_text }}</span> </a>    </span>
{% endif %}
                </div> {% schema %} {
    "name": "B2B Event",
    "settings": [
        {
            "id": "image_bild",
            "type": "image_picker",
            "label": "Bild"
        },
        {
            "id": "text_datum_ort",
            "type": "text",
            "label": "Datum + Ort",
            "default": "15.06.2026 \u00b7 Berlin"
        },
        {
            "id": "text_heading",
            "type": "text",
            "label": "Heading",
            "default": "Event title"
        },
        {
            "id": "textarea_text",
            "type": "textarea",
            "label": "Text",
            "default": "Description of the event and the DICOTA presence."
        },
        {
            "id": "url_button_link",
            "type": "url",
            "label": "Button Link"
        },
        {
            "id": "text_button_text",
            "type": "text",
            "label": "Button Text",
            "default": "Details"
        }
    ],
    "presets": [
        {
            "name": "B2B Event",
            "blocks": []
        }
    ],
    "tag": null
} {% endschema %}