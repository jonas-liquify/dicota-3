<button li-block="Tab Link" type="button" role="tab" class="value-tabs_link" id="tab-{{ forloop.index }}" aria-controls="tabpanel-{{ forloop.index }}" x-on:click="tab = {{ forloop.index }}" x-bind:aria-selected="tab === {{ forloop.index }} ? 'true' : 'false'" x-bind:class="tab === {{ forloop.index }} ? 'is-active' : ''" {{ block.shopify_attributes }}>
                    <div>
{{ block.settings.text_tab_label }}
</div>
                </button> {% schema %} {
    "name": "Tab Link",
    "settings": [
        {
            "id": "text_tab_label",
            "type": "text",
            "label": "Tab Label",
            "default": "Vertrauen"
        }
    ],
    "presets": [
        {
            "name": "Tab Link",
            "blocks": []
        }
    ],
    "tag": null
} {% endschema %}